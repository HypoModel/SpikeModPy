[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "spikemodpy"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["numpy", "wxPython", "hypomodpy"]

[tool.setuptools]
packages = ["SpikeModPy"]
include-package-data = true